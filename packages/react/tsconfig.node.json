{
  // 编译器选项配置，用于指导TypeScript如何编译代码
  "compilerOptions": {
    // 启用复合构建项目的能力，需要在编译时考虑多个项目的依赖关系
    "composite": true,
    // 跳过库文件的类型检查，以加快编译速度
    "skipLibCheck": true,
    // 指定模块系统为ESNext，支持最新的模块语法
    "module": "ESNext",
    // 指定模块解析策略为Node.js风格
    "moduleResolution": "node",
    // 允许从不导出默认值的模块中默认导入，提供向后兼容性
    "allowSyntheticDefaultImports": true,
    // 启用严格的类型检查选项，提升代码质量
    "strict": true,
    // 要求文件名在不同操作系统间保持一致的大小写，确保跨平台兼容性
    "forceConsistentCasingInFileNames": true
  },
  // 指定需要包含在构建中的文件，这里只包含vite配置文件
  "include": ["vite.config.ts"]
}